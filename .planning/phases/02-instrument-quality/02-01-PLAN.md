---
phase: 02-instrument-quality
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - engine/instruments.js
  - engine/effects.js
  - engine/voice-tracker.js
autonomous: true
requirements: [SYNTH-01, SYNTH-02, SYNTH-03, FX-01, FX-02, FX-03, FX-04, FX-05]

must_haves:
  truths:
    - "Synth produces sound through full effects chain (reverb, delay, distortion, filter)"
    - "Changing waveform type audibly changes the synth timbre"
    - "Adjusting ADSR parameters changes the envelope shape audibly"
    - "Filter cutoff and resonance respond in real time"
    - "Each effect has wet/dry or amount control that audibly changes the sound"
    - "Playing 8 notes simultaneously works; a 9th note voice-steals the oldest"
  artifacts:
    - path: "engine/instruments.js"
      provides: "Subtractive synth with waveform selection, ADSR, filter, velocity-aware noteOn/noteOff, voice stealing"
      exports: ["noteOn", "noteOff", "setSynthParam", "getActiveSynth", "releaseAll"]
    - path: "engine/effects.js"
      provides: "Full effects chain: reverb, delay, distortion, filter, channel, masterVolume"
      exports: ["reverb", "delay", "distortion", "filterFX", "channel", "masterVolume", "connectInstrument", "disconnectInstrument", "effectsReady"]
    - path: "engine/voice-tracker.js"
      provides: "Active note tracking array with voice stealing logic"
      exports: ["trackNoteOn", "trackNoteOff", "stealOldestIfFull", "getActiveNotes", "clearAll"]
  key_links:
    - from: "engine/instruments.js"
      to: "engine/effects.js"
      via: "connectInstrument(synth) call"
      pattern: "connectInstrument"
    - from: "engine/instruments.js"
      to: "engine/voice-tracker.js"
      via: "trackNoteOn/stealOldestIfFull before triggerAttack"
      pattern: "stealOldestIfFull|trackNoteOn"
    - from: "engine/effects.js"
      to: "Tone.Destination"
      via: "masterVolume.toDestination()"
      pattern: "toDestination"
---

<objective>
Refactor the Phase 1 single-voice warmPad into a full subtractive synthesizer with selectable waveforms, ADSR envelope, filter with cutoff/resonance, and voice stealing. Replace the simple direct-to-masterVolume signal chain with a full effects bus: reverb, delay, distortion, filter effect, and per-channel volume/pan.

Purpose: This is the audio backbone for all of Phase 2. Every subsequent plan (presets, MIDI, UI controls) depends on these APIs existing.
Output: Refactored engine/instruments.js, engine/effects.js, new engine/voice-tracker.js
</objective>

<execution_context>
@/Users/brad/.claude/get-shit-done/workflows/execute-plan.md
@/Users/brad/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-instrument-quality/02-RESEARCH.md
@.planning/phases/01-audio-foundation/01-01-SUMMARY.md

<interfaces>
<!-- Current codebase contracts that this plan modifies -->

From engine/instruments.js (CURRENT - will be refactored):
```javascript
export const warmPad = new Tone.PolySynth(Tone.Synth, { maxPolyphony: 8, ... });
export function noteOn(note) { warmPad.triggerAttack(note, Tone.now()); }
export function noteOff(note) { warmPad.triggerRelease(note, Tone.now()); }
```

From engine/effects.js (CURRENT - will be refactored):
```javascript
import { warmPad } from './instruments.js';
export const masterVolume = new Tone.Volume(-6).toDestination();
warmPad.connect(masterVolume);
```

From engine/audio-engine.js (UNCHANGED - import only):
```javascript
export async function ensureAudioStarted();
```

From input/keyboard.js and input/touch.js (CONSUMERS - noteOn/noteOff signature must remain compatible):
```javascript
import { noteOn, noteOff } from '../engine/instruments.js';
// keyboard.js calls: noteOn(note)
// touch.js calls: noteOn(note)
```

CRITICAL: noteOn must accept an optional velocity parameter (default 0.8) so existing callers without velocity still work.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create voice tracker and refactor effects chain</name>
  <files>engine/voice-tracker.js, engine/effects.js</files>
  <action>
Create engine/voice-tracker.js:
- Export MAX_VOICES = 8
- Export activeNotes array: [{ note, startedAt }] ordered by time
- Export trackNoteOn(note): push { note, startedAt: Tone.now() } to activeNotes
- Export trackNoteOff(note): find and splice from activeNotes by note name
- Export stealOldestIfFull(): if activeNotes.length >= MAX_VOICES, shift oldest entry and return the stolen note name (caller must triggerRelease it). Return null if not full.
- Export getActiveNotes(): return copy of activeNotes array
- Export clearAll(): empty activeNotes array (used during grid rebuild to prevent stuck notes)

Refactor engine/effects.js to build full effects chain:
- Remove the `import { warmPad }` and `warmPad.connect(masterVolume)` lines
- Create and export: reverb (Tone.Reverb, decay:2.5, wet:0.3), delay (Tone.FeedbackDelay, delayTime:'8n', feedback:0.35, wet:0), distortion (Tone.Distortion, distortion:0.3, wet:0), filterFX (Tone.Filter, frequency:4000, type:'lowpass', Q:1), channel (Tone.Channel, volume:0, pan:0), masterVolume (Tone.Volume, -6dB, toDestination)
- Wire chain: channel.connect(masterVolume), filterFX.connect(channel), distortion.connect(filterFX), delay.connect(distortion), reverb.connect(delay)
- Use top-level await for `await reverb.ready` (valid in ES modules)
- Export connectInstrument(synth): synth.connect(reverb)
- Export disconnectInstrument(synth): synth.disconnect()
- Export effectsReady promise (resolves after reverb.ready) so consumers can gate on it

IMPORTANT: Keep masterVolume export name identical so app.js volume slider wiring still works unchanged.
IMPORTANT: Delay wet starts at 0 (off), distortion wet starts at 0 (off) — only reverb has audible default. This prevents overwhelming effects on first load.
  </action>
  <verify>
    <automated>node -e "import('file:///Users/brad/Apps/Tydal/engine/voice-tracker.js').then(m => { console.log(Object.keys(m)); if(!m.trackNoteOn || !m.stealOldestIfFull || !m.clearAll) throw 'missing exports'; console.log('voice-tracker OK'); })" --input-type=module</automated>
  </verify>
  <done>voice-tracker.js exports trackNoteOn, trackNoteOff, stealOldestIfFull, clearAll. effects.js exports reverb, delay, distortion, filterFX, channel, masterVolume, connectInstrument, disconnectInstrument. Chain wired: reverb -> delay -> distortion -> filterFX -> channel -> masterVolume -> Destination.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor instruments.js with subtractive synth, velocity, and voice stealing</name>
  <files>engine/instruments.js</files>
  <action>
Rewrite engine/instruments.js:
- Import connectInstrument, disconnectInstrument from effects.js
- Import trackNoteOn, trackNoteOff, stealOldestIfFull, clearAll from voice-tracker.js
- Create subtractiveSynth: new Tone.PolySynth(Tone.Synth, { maxPolyphony: 8, options: { oscillator: { type: 'sawtooth' }, envelope: { attack: 0.02, decay: 0.1, sustain: 0.85, release: 0.4 }, filter: { type: 'lowpass', frequency: 2800, Q: 1, rolloff: -12 }, filterEnvelope: { attack: 0.05, decay: 0.2, sustain: 0.5, release: 0.8, baseFrequency: 200, octaves: 3.5 } } })
- Export let activeSynth = subtractiveSynth (mutable — preset switching will change this in Plan 04)
- Connect activeSynth to effects chain via connectInstrument(activeSynth)
- Export noteOn(note, velocity = 0.8):
  1. Call stealOldestIfFull() — if returns a note, call activeSynth.triggerRelease(stolenNote, Tone.now())
  2. Call trackNoteOn(note)
  3. Call activeSynth.triggerAttack(note, Tone.now(), velocity)
- Export noteOff(note):
  1. Call trackNoteOff(note)
  2. Call activeSynth.triggerRelease(note, Tone.now())
- Export releaseAll(): triggerRelease all tracked notes, then clearAll()
- Export setSynthParam(paramObj): activeSynth.set(paramObj) — e.g. setSynthParam({ oscillator: { type: 'square' } })
- Export getActiveSynth(): returns activeSynth reference
- Export switchInstrument(newSynth): releaseAll on old, disconnectInstrument(old), set activeSynth = newSynth, connectInstrument(newSynth)

CRITICAL: noteOn signature is noteOn(note, velocity = 0.8) — the default ensures backward compatibility with keyboard.js and touch.js callers that don't pass velocity yet.
CRITICAL: Do NOT import warmPad anywhere — it no longer exists. The subtractiveSynth replaces it with identical initial sound character.
  </action>
  <verify>
    <automated>cd /Users/brad/Apps/Tydal && python3 -m http.server 8080 &>/dev/null & sleep 1 && curl -s http://localhost:8080/ | grep -c 'importmap' && kill %1 2>/dev/null; echo "Server test done"</automated>
  </verify>
  <done>instruments.js exports noteOn(note, velocity), noteOff(note), releaseAll(), setSynthParam(), switchInstrument(). Voice stealing triggers on 9th simultaneous note. Existing keyboard.js and touch.js callers work without modification (velocity defaults to 0.8). Page loads without console errors.</done>
</task>

</tasks>

<verification>
- Load page in browser, play notes with keyboard — sound should be identical to Phase 1 (same sawtooth/filter/ADSR parameters)
- Hold 8 keys simultaneously, then press a 9th — oldest note should release and 9th should sound
- Open browser console: no import errors, no "warmPad" references
- Volume slider still works (masterVolume export name unchanged)
</verification>

<success_criteria>
- Subtractive synth plays through full effects chain with reverb audible by default
- 8-voice polyphony works; 9th note voice-steals oldest
- noteOn accepts optional velocity parameter (0-1 range)
- setSynthParam can change waveform, ADSR, filter at runtime
- All existing functionality (keyboard play, touch play, volume slider) works unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/02-instrument-quality/02-01-SUMMARY.md`
</output>

---
phase: 02-instrument-quality
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - ui/pad-grid.js
  - input/keyboard.js
  - input/touch.js
  - index.html
  - styles.css
autonomous: true
requirements: [PERF-04, PERF-05, PERF-07]

must_haves:
  truths:
    - "Pads display chromatic semitone labels (C3, C#3, D3, D#3, ...)"
    - "Octave shift buttons move the pad range up or down by one octave"
    - "Pressing 8 pads simultaneously on mobile produces 8 notes without zoom or scroll interference"
  artifacts:
    - path: "ui/pad-grid.js"
      provides: "Chromatic NOTE_MAP builder, octave shift, dynamic grid rebuild"
      exports: ["buildNoteMap", "initPadGrid", "setPadActive", "shiftOctave", "getCurrentOctave", "getNoteMap"]
    - path: "input/touch.js"
      provides: "Multitouch pointer handlers with touch-action enforcement"
    - path: "input/keyboard.js"
      provides: "Keyboard handler updated for dynamic note map"
  key_links:
    - from: "ui/pad-grid.js"
      to: "input/keyboard.js"
      via: "getNoteMap() for dynamic KEY_TO_NOTE rebuilding"
      pattern: "getNoteMap"
    - from: "index.html"
      to: "ui/pad-grid.js"
      via: "Octave shift buttons wired to shiftOctave()"
      pattern: "shiftOctave"
---

<objective>
Replace the diatonic NOTE_MAP with a chromatic 16-note layout (consecutive semitones), add octave shift buttons (+/- octave), and harden multitouch to handle 8 simultaneous fingers without triggering zoom or scroll.

Purpose: A chromatic layout makes the instrument musically useful — every semitone is reachable. Octave shift extends the range beyond 16 notes. Multitouch is essential for chords and polyphonic play on mobile.
Output: Refactored pad-grid.js, keyboard.js, touch.js; octave shift buttons in index.html
</objective>

<execution_context>
@/Users/brad/.claude/get-shit-done/workflows/execute-plan.md
@/Users/brad/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-instrument-quality/02-RESEARCH.md

<interfaces>
<!-- Current contracts from Phase 1 that this plan modifies -->

From ui/pad-grid.js (CURRENT - will be refactored):
```javascript
export const NOTE_MAP = [ { note: 'C3', key: 'z', padIndex: 0 }, ... ]; // 16 diatonic entries
export function initPadGrid(containerEl); // creates DOM, returns grid element
export function setPadActive(note, isActive); // toggles .active class
```

From input/keyboard.js (CURRENT - will be refactored):
```javascript
const KEY_TO_NOTE = Object.fromEntries(NOTE_MAP.map(...)); // static at import time
export function initKeyboard(); // global keydown/keyup listeners
```

From input/touch.js (CURRENT - minor changes):
```javascript
const touchedPads = new Map(); // pointerId -> note
export function initTouch(padGridEl); // pointer event listeners
```

From engine/instruments.js (CONSUMER - noteOn/noteOff called, signature unchanged):
```javascript
export function noteOn(note, velocity = 0.8); // Plan 01 adds velocity param with default
export function noteOff(note);
export function releaseAll(); // Plan 01 adds this — needed before grid rebuild
```

CRITICAL: releaseAll() from Plan 01 must be called before rebuilding the grid to prevent stuck notes (Pitfall 5 in RESEARCH.md). If Plan 01 hasn't executed yet when this plan runs, use a fallback that iterates touchedPads and heldKeys to release manually.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor pad-grid.js for chromatic layout and octave shift</name>
  <files>ui/pad-grid.js, index.html, styles.css</files>
  <action>
Rewrite ui/pad-grid.js:
- Define CHROMATIC = ['C', 'C#', 'D', 'D#', 'E', 'F', 'F#', 'G', 'G#', 'A', 'A#', 'B']
- Define KEYS = ['z','x','c','v', 'a','s','d','f', 'q','w','e','r', '1','2','3','4'] (same mapping as Phase 1)
- Export let currentOctave = 3 (C3 = bottom-left pad)
- Export function buildNoteMap(octave = currentOctave): generates 16 entries of { note, key, padIndex } using consecutive chromatic semitones starting at C{octave}. When chromIdx reaches 12, reset to 0 and increment octave.
- Export function getNoteMap(): returns current note map array
- Export function getCurrentOctave(): returns currentOctave
- Export function shiftOctave(delta): clamp currentOctave to 1-7 range, call rebuildGrid(). IMPORTANT: Before rebuilding, release all active notes — import releaseAll from instruments.js if available, otherwise manually iterate and call noteOff on any active pads. Clear touchedPads and heldKeys state by dispatching a custom 'grid-rebuild' event that input modules listen to.
- Refactor initPadGrid(containerEl): store reference to containerEl, call buildNoteMap() to get notes, render grid with MPC layout (same reversed-row approach). Store the grid element reference for rebuildGrid.
- Internal function rebuildGrid(): clear grid innerHTML, regenerate pads from buildNoteMap(currentOctave), dispatch 'grid-rebuild' event so keyboard.js rebuilds its KEY_TO_NOTE map.
- Export setPadActive(note, isActive): same as before but querySelectorAll on current grid.

Add to index.html: octave shift buttons in the controls-bar area (between instrument container and volume control):
```html
<div class="octave-control">
  <button id="octave-down" class="octave-btn" aria-label="Octave down">-</button>
  <span id="octave-display" class="octave-display">Oct 3</span>
  <button id="octave-up" class="octave-btn" aria-label="Octave up">+</button>
</div>
```

Add to styles.css: octave control styles — flex row, buttons styled like help-btn (32x32 circle, bg-surface, border-subtle), octave-display is text-secondary 0.75rem.

Wire octave buttons in app.js is NOT needed — pad-grid.js will export shiftOctave and app.js will wire it. Actually, add the wiring to pad-grid.js's initPadGrid since it owns the grid: after creating the grid, attach click listeners to #octave-down and #octave-up that call shiftOctave(-1) and shiftOctave(+1) respectively, and update #octave-display text.
  </action>
  <verify>
    <automated>cd /Users/brad/Apps/Tydal && node -e "
const fs = require('fs');
const html = fs.readFileSync('index.html', 'utf8');
const pg = fs.readFileSync('ui/pad-grid.js', 'utf8');
let ok = true;
if (!html.includes('octave-down')) { console.error('Missing octave-down button'); ok = false; }
if (!html.includes('octave-up')) { console.error('Missing octave-up button'); ok = false; }
if (!pg.includes('CHROMATIC')) { console.error('Missing CHROMATIC array'); ok = false; }
if (!pg.includes('shiftOctave')) { console.error('Missing shiftOctave export'); ok = false; }
if (!pg.includes('buildNoteMap')) { console.error('Missing buildNoteMap export'); ok = false; }
if (ok) console.log('pad-grid chromatic refactor OK');
process.exit(ok ? 0 : 1);
"</automated>
  </verify>
  <done>pad-grid.js generates chromatic 16-note map starting at C{currentOctave}. Pads show semitone labels (C3, C#3, D3, ...). Octave shift buttons in HTML wired to shiftOctave. Grid rebuilds on octave change without stuck notes.</done>
</task>

<task type="auto">
  <name>Task 2: Update keyboard and touch input for dynamic note map</name>
  <files>input/keyboard.js, input/touch.js</files>
  <action>
Refactor input/keyboard.js:
- Instead of building KEY_TO_NOTE once at import from a static NOTE_MAP, build it dynamically from getNoteMap().
- Export a rebuildKeyMap() function that reconstructs KEY_TO_NOTE from getNoteMap()
- On 'grid-rebuild' event (dispatched by pad-grid.js on octave shift), call rebuildKeyMap() and clear heldKeys Set
- In initKeyboard(), call rebuildKeyMap() once to initialize, then add document.addEventListener('grid-rebuild', ...) listener

Refactor input/touch.js:
- On 'grid-rebuild' event, clear touchedPads Map (all pointers invalidated by grid rebuild)
- Ensure touch-action: none is set on the grid element AND on body during touch interactions to prevent any zoom/scroll
- Add explicit CSS property: body style touch-action: manipulation (set once in initTouch)
- The pointerdown handler already uses e.preventDefault() with passive:false — verify this is preserved
- For multitouch hardening: ensure no event handler calls stopPropagation unnecessarily, which could break concurrent pointer tracking

No changes to noteOn/noteOff call signatures — they still call noteOn(note) without velocity. Velocity comes in Plan 03.
  </action>
  <verify>
    <automated>cd /Users/brad/Apps/Tydal && node -e "
const fs = require('fs');
const kb = fs.readFileSync('input/keyboard.js', 'utf8');
const tc = fs.readFileSync('input/touch.js', 'utf8');
let ok = true;
if (!kb.includes('grid-rebuild')) { console.error('keyboard.js missing grid-rebuild listener'); ok = false; }
if (!kb.includes('getNoteMap') && !kb.includes('rebuildKeyMap')) { console.error('keyboard.js missing dynamic note map'); ok = false; }
if (!tc.includes('grid-rebuild')) { console.error('touch.js missing grid-rebuild listener'); ok = false; }
if (ok) console.log('input handlers dynamic note map OK');
process.exit(ok ? 0 : 1);
"</automated>
  </verify>
  <done>Keyboard handler rebuilds KEY_TO_NOTE on octave shift. Touch handler clears touchedPads on grid rebuild. Both clear held state on 'grid-rebuild' event. Multitouch works for 8 simultaneous pointers without zoom/scroll interference.</done>
</task>

</tasks>

<verification>
- Load page, verify pads show chromatic labels: C3, C#3, D3, D#3, E3, F3, F#3, G3, G#3, A3, A#3, B3, C4, C#4, D4, D#4
- Click octave up: labels shift to C4, C#4, ... D#5. Click octave down: back to C3 range.
- Hold a key, click octave shift — no stuck note
- On mobile (or simulated): press 4+ pads simultaneously — all play, no zoom triggered
</verification>

<success_criteria>
- 16 pads show consecutive chromatic semitones starting at C3
- Octave +/- buttons shift range by one octave, clamped 1-7
- Keyboard mapping updates dynamically after octave shift
- No stuck notes on octave shift during play
- 8 simultaneous touches produce 8 notes on mobile
</success_criteria>

<output>
After completion, create `.planning/phases/02-instrument-quality/02-02-SUMMARY.md`
</output>

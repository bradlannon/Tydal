---
phase: 08-multi-track-system
plan: "03"
type: execute
wave: 3
depends_on: ["08-02"]
files_modified:
  - public/engine/track-manager.js
  - public/engine/drums.js
  - public/engine/melodic-sequencer.js
  - public/ui/track-buttons.js
  - public/ui/encoder-row.js
  - public/styles.css
autonomous: true
requirements: [MTRK-04, MTRK-05]

must_haves:
  truths:
    - "Each melodic track can have reverb and delay applied independently"
    - "Switching tracks does not bleed effects between tracks"
    - "Per-track volume is adjustable via encoder or track button interaction"
    - "Muting a track silences it without stopping the sequencer"
  artifacts:
    - path: "public/engine/track-manager.js"
      provides: "setTrackVolume and setTrackMute helpers"
      exports: ["setTrackVolume", "setTrackMute"]
    - path: "public/ui/encoder-row.js"
      provides: "Per-track volume encoder in MELODIC_MAPPING"
    - path: "public/ui/track-buttons.js"
      provides: "Volume indicator on track buttons"
  key_links:
    - from: "public/ui/track-buttons.js"
      to: "public/engine/track-manager.js"
      via: "setTrackMute on long-press"
      pattern: "setTrackMute"
    - from: "public/engine/melodic-sequencer.js"
      to: "public/engine/track-manager.js"
      via: "track.muted check in Sequence callback"
      pattern: "track\\.muted"
    - from: "public/engine/drums.js"
      to: "public/engine/track-manager.js"
      via: "Drum bus volume reads from track 0 volume"
      pattern: "getTrackById\\(0\\)"
---

<objective>
Complete per-track effects isolation, volume control, and mute functionality so that each track operates independently in the mix.

Purpose: This closes the multi-track system — tracks are not just switchable but truly independent audio channels with their own effects and mixing controls.

Output: Updated `track-manager.js`, `drums.js`, `melodic-sequencer.js`, `track-buttons.js`, `encoder-row.js`, `styles.css`
</objective>

<execution_context>
@/Users/brad/.claude/get-shit-done/workflows/execute-plan.md
@/Users/brad/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-multi-track-system/08-01-SUMMARY.md
@.planning/phases/08-multi-track-system/08-02-SUMMARY.md

@public/engine/track-manager.js
@public/engine/drums.js
@public/engine/melodic-sequencer.js
@public/ui/track-buttons.js
@public/ui/encoder-row.js

<interfaces>
<!-- From 08-01 and 08-02 -->
From public/engine/track-manager.js:
```javascript
export const TRACK_COLORS;
export const tracks;
export function getActiveTrack();
export function setActiveTrack(id);
export function getTrackById(id);
export function getActiveTrackId();
// Each track: { id, color, type, synth?, effectsChain?, grid?, muted, volume }
// effectsChain: { input, reverb, delay, channel, ready }
```

From public/engine/drums.js:
```javascript
export const drumBus;  // Tone.Channel routed to masterVolume
```

From public/ui/encoder-row.js:
```javascript
export function setEncoderMapping(mappingArray);
// buildTrackMelodicMapping(track) from 08-02
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Per-track volume/mute helpers and drum track volume wiring</name>
  <files>public/engine/track-manager.js, public/engine/drums.js, public/engine/melodic-sequencer.js</files>
  <action>
**Modify `public/engine/track-manager.js`:**

1. Add `setTrackVolume(trackId, volumeDb)` function:
   - Updates `tracks[trackId].volume = volumeDb`
   - If drum track (0): import and call `drumBus.volume.rampTo(volumeDb, 0.05)`
   - If melodic track: call `tracks[trackId].effectsChain.channel.volume.rampTo(volumeDb, 0.05)`
   - Dispatch `CustomEvent('track-volume', { detail: { trackId, volume: volumeDb } })`

2. Add `setTrackMute(trackId, muted)` function:
   - Updates `tracks[trackId].muted = muted`
   - If drum track (0): call `drumBus.mute = muted`
   - If melodic track: call `tracks[trackId].effectsChain.channel.mute = muted`
   - Dispatch `CustomEvent('track-mute', { detail: { trackId, muted } })`

3. Export both functions.

**Verify `public/engine/melodic-sequencer.js`** already checks `track.muted` in the Sequence callback from 08-01. If not, add the check: skip `triggerAttackRelease` for muted melodic tracks.

**Verify `public/engine/sequencer.js`** already checks drum track mute from 08-01. The `drumBus.mute` approach in track-manager.js is cleaner — using Tone.Channel's built-in mute means the sequencer.js callback doesn't need to check mute at all (audio is silenced at the channel level). If 08-01 added an explicit mute check in sequencer.js, keep it as belt-and-suspenders. But the primary mute mechanism should be `drumBus.mute = true/false` set by `setTrackMute`.

**Modify `public/engine/drums.js`** (minor):

1. Ensure `drumBus` is accessible for volume/mute control. It's already exported. No changes needed unless 08-01 missed something.
  </action>
  <verify>
    <automated>cd /Users/brad/Apps/Tydal && grep -n "setTrackVolume\|setTrackMute" public/engine/track-manager.js | head -6 && grep -n "track.muted\|\.muted" public/engine/melodic-sequencer.js | head -4 && echo "PASS"</automated>
  </verify>
  <done>setTrackVolume and setTrackMute exported from track-manager.js. Drum track volume goes through drumBus. Melodic track volume goes through each track's channel. Muting a track silences it at the audio channel level without stopping the sequencer.</done>
</task>

<task type="auto">
  <name>Task 2: Track button volume/mute indicators and encoder volume per-track</name>
  <files>public/ui/track-buttons.js, public/ui/encoder-row.js, public/styles.css</files>
  <action>
**Modify `public/ui/track-buttons.js`:**

1. Import `setTrackMute, getTrackById` from `../engine/track-manager.js`.

2. Update the long-press mute handler: call `setTrackMute(trackId, !track.muted)` instead of directly setting `track.muted`.

3. Listen for `track-mute` CustomEvent to update the `.muted` class on the correct button (in case mute is toggled from elsewhere).

4. Add a small visual mute indicator: when muted, add a tiny diagonal strikethrough line via CSS pseudo-element (or just use opacity 0.3 which was already planned in 08-02).

**Modify `public/ui/encoder-row.js`:**

1. Ensure `buildTrackMelodicMapping(track)` (from 08-02) includes a "Trk Vol" encoder that calls `setTrackVolume(track.id, val)` from track-manager.js. Import `setTrackVolume` from track-manager.js.

2. The mapping should include:
   - Cutoff (track.synth filter), Res (track.synth filter Q), Reverb (track.effectsChain.reverb.wet), Delay (track.effectsChain.delay.wet), Attack (track.synth envelope), Release (track.synth envelope), Dist (no per-track distortion — use a placeholder or skip), Trk Vol (setTrackVolume), Master Vol (masterVolume).

3. For the DRUM_MAPPING: replace the "Drum Vol" encoder's apply to use `setTrackVolume(0, val)` instead of directly calling `drumBus.volume.rampTo`. This ensures the track-manager state stays in sync.

**Modify `public/styles.css`:**

1. Add `.track-btn.muted::after` pseudo-element: a thin diagonal line (2px white, 50% opacity, rotated 45deg) across the button to indicate mute. OR simply use the opacity approach (`.track-btn.muted { opacity: 0.25; }`).

2. Ensure track button active state has sufficient contrast — the active track's border should glow subtly in the track color.
  </action>
  <verify>
    <automated>cd /Users/brad/Apps/Tydal && grep -n "setTrackVolume\|setTrackMute" public/ui/track-buttons.js public/ui/encoder-row.js | head -8 && grep -n "track-btn.muted\|\.muted" public/styles.css | head -4 && echo "PASS"</automated>
  </verify>
  <done>Track buttons show mute state visually. Encoder row includes per-track volume control. Drum volume encoder routes through setTrackVolume for state consistency. Muting and volume changes work from both track buttons and encoders.</done>
</task>

</tasks>

<verification>
1. Long-press a track button dims it (mute) — that track goes silent but sequencer continues
2. Long-press again un-mutes — sound returns
3. Turning the Trk Vol encoder on a melodic track changes only that track's volume
4. Switching to a different track and adjusting effects (reverb/delay) does not affect the previous track's sound
5. Playing all 4 tracks simultaneously — each has its own reverb/delay character
6. Muting drum track (track 0) silences drums but melodic tracks continue
</verification>

<success_criteria>
- Per-track volume adjustable via encoders
- Per-track mute via long-press on track buttons
- Effects don't bleed between tracks (each melodic track has independent reverb/delay)
- All controls route through track-manager.js for consistent state
</success_criteria>

<output>
After completion, create `.planning/phases/08-multi-track-system/08-03-SUMMARY.md`
</output>

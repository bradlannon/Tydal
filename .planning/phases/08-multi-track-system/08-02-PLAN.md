---
phase: 08-multi-track-system
plan: "02"
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - public/ui/track-buttons.js
  - public/ui/pad-grid.js
  - public/ui/step-buttons.js
  - public/ui/encoder-row.js
  - public/index.html
  - public/styles.css
  - public/app.js
autonomous: true
requirements: [MLAY-04, MVIS-06, MTRK-02]

must_haves:
  truths:
    - "Four track buttons are visible with distinct color indicators"
    - "Tapping a track button switches the active track"
    - "Pad grid immediately reflects the selected track's instrument and color"
    - "Step buttons show the selected track's step pattern"
    - "Encoders auto-map to the selected track's parameters"
  artifacts:
    - path: "public/ui/track-buttons.js"
      provides: "4 track selection buttons with color indicators"
      exports: ["initTrackButtons"]
    - path: "public/ui/pad-grid.js"
      provides: "Track-aware pad coloring using active track color"
    - path: "public/ui/step-buttons.js"
      provides: "Track-aware step display reading active track grid"
  key_links:
    - from: "public/ui/track-buttons.js"
      to: "public/engine/track-manager.js"
      via: "setActiveTrack(id) on button tap"
      pattern: "setActiveTrack"
    - from: "public/ui/pad-grid.js"
      to: "public/engine/track-manager.js"
      via: "Reads active track color for pad coloring"
      pattern: "getActiveTrack.*color"
    - from: "public/ui/step-buttons.js"
      to: "public/engine/track-manager.js"
      via: "track-change event refreshes step display"
      pattern: "track-change"
---

<objective>
Add 4 track selection buttons and wire track switching throughout the UI — pad colors change to the active track's color, step buttons show the active track's pattern, and encoders map to the active track's parameters.

Purpose: This makes the multi-track engine visible and controllable. Users can see and switch between tracks.

Output: `ui/track-buttons.js` (new), modified `pad-grid.js`, `step-buttons.js`, `encoder-row.js`, `index.html`, `styles.css`, `app.js`
</objective>

<execution_context>
@/Users/brad/.claude/get-shit-done/workflows/execute-plan.md
@/Users/brad/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-multi-track-system/08-01-SUMMARY.md

@public/ui/pad-grid.js
@public/ui/step-buttons.js
@public/ui/encoder-row.js
@public/app.js
@public/index.html
@public/styles.css

<interfaces>
<!-- From 08-01: track-manager.js exports -->
From public/engine/track-manager.js (created in 08-01):
```javascript
export const TRACK_COLORS = ['#e87a20', '#00b3f4', '#b44aff', '#00e676'];
export const tracks = [ /* 4 track objects */ ];
export function getActiveTrack();      // returns { id, color, type, synth, effectsChain, grid, ... }
export function setActiveTrack(id);    // dispatches 'track-change' CustomEvent
export function getTrackById(id);
export function getActiveTrackId();
// CustomEvent 'track-change' dispatched with { detail: { trackId } }
```

From public/ui/pad-grid.js:
```javascript
export const TRACK_COLOR = '#e87a20';  // Will be replaced with dynamic track color
export function initPadGrid(containerEl);
export function setPadActive(note, isActive);
export function shiftOctave(delta);
export function setScaleLock(key, scaleName);
export function buildNoteMap(octave?);
export function getNoteMap();
```

From public/ui/step-buttons.js:
```javascript
export function initStepButtons(containerEl);
// Currently reads from melodic-sequencer getSelectedNote/hasNoteAtStep
```

From public/ui/encoder-row.js:
```javascript
export function initEncoderRow(containerEl);
export function setEncoderMapping(mappingArray);
export const MELODIC_MAPPING;
export const DRUM_MAPPING;
// Listens for 'mode-change' CustomEvent
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create track-buttons.js and add to layout</name>
  <files>public/ui/track-buttons.js, public/index.html, public/styles.css</files>
  <action>
**Create `public/ui/track-buttons.js`:**

1. Import `TRACK_COLORS, tracks, getActiveTrackId, setActiveTrack` from `../engine/track-manager.js`.

2. `initTrackButtons(containerEl)` function:
   - Create a vertical column of 4 track buttons (div.track-buttons-col)
   - Each button: `button.track-btn` with `data-track-id="0|1|2|3"`
   - Each button shows a small colored circle indicator (div.track-color-dot) using the track's color as background
   - Track 0 (drum) label: "D" — Tracks 1-3 labels: "1", "2", "3"
   - Active track button gets `.active` class (brighter border in track color)
   - On click: call `setActiveTrack(id)`
   - Listen for `track-change` CustomEvent to update `.active` class
   - On initial render, mark track matching `getActiveTrackId()` as active

3. Long-press behavior for mute (simple toggle):
   - On pointerdown, start a 500ms timer
   - If pointerup fires before 500ms, it's a tap (track switch)
   - If 500ms elapses, toggle `track.muted` and add/remove `.muted` class (dim opacity)
   - Dispatch `CustomEvent('track-mute', { detail: { trackId, muted } })` for sequencer.js/melodic-sequencer.js to pick up

Export: `initTrackButtons`

**Modify `public/index.html`:**

1. Add a `<div id="track-buttons"></div>` inside the instrument-container, BEFORE the encoder-section div. This places the track buttons on the left side of the layout.

**Modify `public/styles.css`:**

1. `.track-buttons-col`: flex column, gap 4px, align self to stretch vertically alongside the main content. Position it on the left side of the instrument-container by changing instrument-container to `display: flex; flex-direction: row`. The track buttons column is `flex-shrink: 0; width: 36px;`. The existing content (encoder-section + pad grid + visualizer) wraps in an inner flex column that gets `flex: 1`.

   IMPORTANT: The instrument-container is currently `flex-direction: column`. Changing to row requires wrapping the existing children (encoder-section, pad grid, visualizer) in a new inner div. Do this in index.html by adding `<div class="instrument-main">` wrapping encoder-section and visualizer (pad grid is injected by JS).

2. `.track-btn`: 36px x 36px (or similar compact square), background #111, border: 2px solid #333, border-radius 6px. Touch-action: manipulation.

3. `.track-btn.active`: border-color set to the track's color (use CSS custom property `--track-color` set via inline style on each button). Background subtle tint of track color at ~10% opacity.

4. `.track-btn.muted`: opacity 0.3.

5. `.track-color-dot`: 8px x 8px circle centered in button, background = track color.

6. `.track-btn-label`: tiny 8px font below dot, color #666, uppercase.

7. `@media (min-width: 768px)` — track buttons 44px x 44px on desktop.
  </action>
  <verify>
    <automated>cd /Users/brad/Apps/Tydal && grep -c "track-btn" public/styles.css && grep -c "track-buttons" public/index.html && grep -c "initTrackButtons" public/ui/track-buttons.js && echo "PASS"</automated>
  </verify>
  <done>4 track buttons visible in a vertical column on the left side of the instrument. Each button has the track's color indicator. Tapping switches active track (dispatches track-change). Long-press toggles mute.</done>
</task>

<task type="auto">
  <name>Task 2: Wire track switching to pad-grid, step-buttons, encoder-row, and app.js</name>
  <files>public/ui/pad-grid.js, public/ui/step-buttons.js, public/ui/encoder-row.js, public/app.js</files>
  <action>
**Modify `public/ui/pad-grid.js`:**

1. Import `getActiveTrack` from `../engine/track-manager.js`.

2. Remove the static `export const TRACK_COLOR = '#e87a20'`. Replace all references to `TRACK_COLOR` with `getActiveTrack().color`. The `_applyPadColors` function should read the active track's color for root note coloring.

3. Listen for `track-change` CustomEvent: when track changes, call `_applyPadColors()` to recolor pads with the new track's color.

4. When track 0 (drum) is active: pads should display drum labels (kick, snare, hihat, clap) on the bottom 4 pads, or simply keep the chromatic layout but the color changes to track 0's color. For simplicity, keep chromatic layout on all tracks — the drum sequencer grid is in the step-buttons area. Pad coloring just uses the active track's color for root notes.

5. `setPadActive` — the white flash behavior stays the same. The role color restore reads `getActiveTrack().color` for root notes.

**Modify `public/ui/step-buttons.js`:**

1. Import `getActiveTrack, getActiveTrackId` from `../engine/track-manager.js`.
2. Import `getStep, ROWS` from `../engine/sequencer.js` (drum grid access).

3. Listen for `track-change` CustomEvent: call `_refreshButtons()` to show the newly selected track's step pattern.

4. Update `_refreshButtons()`:
   - If active track is drum (type === 'drum'): show drum step data. For each button, check if ANY drum row has that step active (`ROWS.some(row => getStep(row, step))`). Active = white, playhead = green. This gives a combined view of the drum pattern.
   - If active track is melodic: existing behavior — check `hasNoteAtStep(i, note)` for the selected note.

5. Update `_onStepTap()`:
   - If active track is drum: don't toggle melodic steps. Instead, for now, do nothing on tap (drum step editing stays in the DRM bottom sheet). OR toggle the last-tapped drum row. For simplicity, make drum step taps toggle ALL drum rows (not useful) — actually better: when drum track is active, step button taps are no-ops. The drum grid is edited via the DRM sheet.
   - If active track is melodic: existing behavior (toggleStep for selected note).

**Modify `public/ui/encoder-row.js`:**

1. Import `getActiveTrack` from `../engine/track-manager.js`.

2. Listen for `track-change` CustomEvent in addition to `mode-change`:
   - When track changes to drum (track 0): apply DRUM_MAPPING
   - When track changes to melodic (tracks 1-3): apply a track-specific MELODIC_MAPPING that wires to that track's effects chain. Create a helper `buildTrackMelodicMapping(track)` that returns a MELODIC_MAPPING-shaped array but with `apply()` functions targeting `track.effectsChain.reverb`, `track.effectsChain.delay`, `track.effectsChain.channel`, and `track.synth` for synth params.
   - The first time (default), use the existing global MELODIC_MAPPING (which targets the global effects — this is fine for Track 1 since Track 1's synth is wired through the global chain initially). For tracks 2 and 3, build dynamic mappings from their track objects.

3. Actually, simpler approach: since 08-01 creates per-track effects chains, ALL melodic tracks should get dynamic mappings. Create `buildTrackMelodicMapping(track)` that reads `track.effectsChain` and `track.synth` to build the 9 encoder params. This replaces the static MELODIC_MAPPING usage for melodic tracks. Keep MELODIC_MAPPING as a fallback/reference.

**Modify `public/app.js`:**

1. Import `initTrackButtons` from `./ui/track-buttons.js`.
2. Call `initTrackButtons(document.getElementById('track-buttons'))` during init.
3. The existing `dispatchModeChange` / `mode-change` system can coexist with `track-change`. When a track button is pressed, `track-change` fires. When a toolbar sheet opens (DRM), `mode-change` fires. Both events are handled by encoder-row.js.
4. Update the DRM sheet open logic: when DRM sheet opens, also call `setActiveTrack(0)` to switch to drum track. When DRM sheet closes, switch back to the previously active melodic track (store the last melodic track id before switching to drums).
  </action>
  <verify>
    <automated>cd /Users/brad/Apps/Tydal && grep -c "track-change" public/ui/pad-grid.js public/ui/step-buttons.js public/ui/encoder-row.js && grep -c "initTrackButtons" public/app.js && echo "PASS"</automated>
  </verify>
  <done>Tapping a track button switches pad colors to the track's color, step buttons show that track's pattern, and encoders map to that track's synth/effects parameters. DRM sheet auto-switches to drum track. All 4 tracks are navigable via the track buttons.</done>
</task>

</tasks>

<verification>
1. 4 colored track buttons appear vertically on the left of the instrument
2. Tapping Track 1 (orange) colors root pads orange; tapping Track 2 (blue) colors them blue
3. Step buttons show different patterns per track
4. Encoders show track-specific parameter names when a melodic track is selected
5. DRM sheet opens with drum track active, closes returning to previous melodic track
6. Long-press on a track button toggles mute (dimmed visual)
</verification>

<success_criteria>
- 4 track buttons visible with distinct colors matching TRACK_COLORS
- Track switching changes pad color, step pattern, and encoder mapping
- No regressions in pad interaction, step toggling, or encoder rotation
</success_criteria>

<output>
After completion, create `.planning/phases/08-multi-track-system/08-02-SUMMARY.md`
</output>
